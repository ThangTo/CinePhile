# Redis settings for a small single-node VPS.
# Keep Redis private to the VPS and use REDIS_URL=redis://127.0.0.1:6379.

bind 127.0.0.1 ::1
protected-mode yes

maxmemory 256mb
maxmemory-policy noeviction

appendonly yes
appendfsync everysec

tcp-keepalive 300
timeout 0
databases 16
